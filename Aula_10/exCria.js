// Crie um programa que peça o número de tarefas que a pessoa quer organizar e depois peça para inserir o nome da tarefa e o horário. Depois o programa exibe as tarefas por ordem de horário.
// numa tabela
var prompt = require('prompt-sync')();

let tarefas = prompt("Digite as tarefas que deseja inserir: ");
let horarios = prompt("Que horario deseja realizar a tarefa: ");

var matriz = [];

for (let t = 0; t < tarefas.length; t++) {
    const cols = [];
    for(let h = 0; h < horarios.length; h++ )
    const hora = horarios[i]
    
    
}
console.table(`${tarefas}  ${horarios}`);